# Phase 7 is enabled and automatic by default.
PHASE7_ENABLED=true
PHASE7_AUTO_RUN_AFTER_CAPTURE=true
PHASE7_DEBOUNCE_SECONDS=2
PHASE7_LOAD_CLINICAL_CONTEXT=true

# Research-only behavior. The background capture has no HTTP
# request cookie, so it may use the newest valid in-memory
# Oracle SMART session. Set false for a multi-user production
# environment until incidents are explicitly user/patient bound.
PHASE7_ALLOW_LATEST_ORACLE_SESSION=true

# Prompt creation is automatic. Actual model inference is off
# until a model endpoint is configured.
PHASE7_RUN_SLM_AUTOMATICALLY=false

# Optional OpenAI-compatible local/private SLM endpoint.
SLM_ENABLED=false
SLM_BASE_URL=http://127.0.0.1:11434/v1
SLM_CHAT_PATH=/chat/completions
SLM_MODEL=
SLM_API_KEY=
SLM_TIMEOUT_SECONDS=90
SLM_MAX_OUTPUT_TOKENS=1200
