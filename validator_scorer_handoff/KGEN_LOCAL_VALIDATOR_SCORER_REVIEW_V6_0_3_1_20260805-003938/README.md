# KGEN local validator/scorer V6.0.3

This CLI runs the active backend validator and scorer after Lightning inference.
It does not load MedGemma and does not place the validator inside the model prompt.
